import '@relmify/jest-fp-ts';

describe('Regex', () => {
	describe('captureFromRegex', () => {
		it('has match', () => {
			throw new Error();
		});

		it('has no match', () => {
			throw new Error();
		});
	});
});
