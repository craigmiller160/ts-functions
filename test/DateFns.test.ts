export {};

describe('DateFns', () => {
	it('addMinutes', () => {
		throw new Error();
	});

	it('format', () => {
		throw new Error();
	});

	it('parse', () => {
		throw new Error();
	});

	it('compareAsc', () => {
		throw new Error();
	});
});
