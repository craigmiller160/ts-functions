export {};

describe('ReadonlyArrayExt', () => {
	describe('dropFirstMatch', () => {
		it('drops the match', () => {
			throw new Error();
		});

		it('has no match', () => {
			throw new Error();
		});
	});

	describe('updateFirstMatch', () => {
		it('updates the match', () => {
			throw new Error();
		});

		it('has no match', () => {
			throw new Error();
		});
	});
});
