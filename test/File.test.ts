export {};

describe('File', () => {
	it('readFileSync', () => {
		throw new Error();
	});

	it('existsSync', () => {
		throw new Error();
	});

	it('rmIfExistsSync', () => {
		throw new Error();
	});

	it('mkdirSync', () => {
		throw new Error();
	});

	it('listFilesSync', () => {
		throw new Error();
	});
});
